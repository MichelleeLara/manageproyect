---
const { techs, bgShadow  } = Astro.props

console.log(techs)
---


{
    techs.map((tech)  =>(
        <div class=" bg-[#202223] flex items-center justify-center gap-3 py-1 px-2 rounded-lg">
            <div style={{backgroundColor:bgShadow}} class="w-2 h-2 rounded-full"></div>
            <p class="text-gray-400 text-xs whitespace-nowrap">{tech}</p>
        </div>
    ))
}

